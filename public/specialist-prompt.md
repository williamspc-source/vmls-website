My recommendation: do the Specialists experience as a vertical slice. It turns the data you just modelled into the site's highest-value, most interactive journey (browse → filter by specialty → profile → see availability → email enquiry), it completes the availability feature you just built, and it forces us to build the reusable patterns (data-driven listings, taxonomy filters, detail pages, cards) that the rest of the site reuses.